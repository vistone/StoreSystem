# Worker — 极简启动配置（默认 worker.yaml）
mode: worker
global:
  max_message_size: 268435456
  protocol: "both"
worker:
  worker_id: "worker-0"
  listen_addr: "0.0.0.0:50161"
  master_addr: "http://127.0.0.1:50051"
  data_dir: "worker_data/worker-0"
